<%= form_for(page_document) do |f| %>
  <% if page_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_document.errors.count, "error") %> prohibited this page_document from being saved:</h2>

      <ul>
      <% page_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :admin_user %>
    <%= f.number_field :admin_user %>
  </div>

  <div class="field">
    <%= f.label :file %>
    <%= f.file_field :file %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
