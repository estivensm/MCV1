 <% @sources.each do |source| %>
      <tr>
        <td><%= source.name %></td>
        <td><%= source.description %></td>
        <td><%= source.user_id %></td>
        <td><%= source.admin_user %></td>
        <td><%= source.state %></td>
        <%if params[:page] == nil
          @a = 1
        else
        @a = params[:page]
        end
        if (params[:search] == nil || params[:search] == "") && @ja == nil
          @b = "$%$"
        else
        @b = params[:search]
        end

          %>
        <td><%= link_to  change_path(source.id,@a,@b), remote:true , :class => "opciones" do %>
               <% if source.default %>
               <i class="fa fa-hand-peace-o def" aria-hidden="true"></i>
               <%else%>
               <i class="fa fa-hand-rock-o" aria-hidden="true"></i>
               
               <%end%>
             </td>

               
               <% end %>
        <td><%= link_to  source, :class => "opciones" do %>
               <i class="fa fa-eye" aria-hidden="true"></i>
               <% end %>
               <%= link_to  edit_source_path(source), remote:true, :class => "opciones" do %>
               <i class="fa fa-pencil" aria-hidden="true"></i>
               <% end %>
               <%= link_to  source,  method: :delete, data: { confirm: 'Are you sure?' }, :class => "opciones" do %>
               <i class="fa fa-trash" aria-hidden="true"><% end %></td>
      </tr>
    <% end %>