             <%if reportss.count > 0 %>
            
            <% i = 0  
            reportss.each do |report|%>
             
  
                <%if report_array[i]["cierre"] || accion_array[i]["cierre"] ||  task_array[i]["cierre"]%>

                    <%= render "report_table", report: report, employed_p: report.employed_reporta, desc: "Asignado por", i: i%>
       
                 <%end%>

            <% i = i + 1 
            end%>
       
           

               <%end%>